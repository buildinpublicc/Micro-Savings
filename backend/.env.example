# If EADDRINUSE: kill $(lsof -t -i :3001)  or  PORT=3002
PORT=3001
DATABASE_PATH=./data/micro-savings.sqlite
# Standard 5-field cron: every 5 minutes
CRON_SCHEDULE=*/5 * * * *
# Comma-separated origins for CORS (Vite default)
CORS_ORIGIN=http://localhost:5173
